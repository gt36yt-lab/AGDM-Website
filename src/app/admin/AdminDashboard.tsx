"use client";

export default function AdminDashboard() {
  return (
    <main className="min-h-screen w-full bg-white">
      <iframe
        src="/images/admin-page.html"
        title="Admin dashboard"
        className="h-screen w-full border-0"
        style={{ display: "block" }}
      />
    </main>
  );
}
